package exporter

// Export is a sample function where you can start working on this challenge.
func Export(csvFilePath, outputDirectoryPath string) error {
	_ = csvFilePath
	_ = outputDirectoryPath

	return nil
}
